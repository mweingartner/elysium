# Security Policy

## What Elysium is (and isn't)

Elysium is a local macOS game with player-started LAN multiplayer session support. Local-network multiplayer is deliberately scoped to Bonjour discovery, Direct Connect, bounded handshakes, peer status, LAN chat, host-authoritative replication batches, and host-authorized gameplay intents. Clients can request actions with typed intents, but they cannot send raw save data, arbitrary commands, authoritative RPG state, or authoritative world state; the remote-player gameplay layer enforces permissions for build, container, crafting, template, command, AI, creative, dimension, respawn, death, and reconnect flows.

Protocol 5 is a trusted-private-LAN transport, not a hostile-network boundary. Its strict frame
sequences, closed phase/direction policy, pre-authentication 4 KiB header cap, socket caps,
per-category guest rate limits, and host-bound identity claims prevent accidental and many active
misuses, but frames remain plaintext. Durable reconnect state additionally requires a host-issued
256-bit capability; UUID possession alone is insufficient and legacy capability-less records are
not restored. Because that capability is visible to a LAN sniffer and stored in user defaults,
protocol 6 still requires authenticated encryption, authenticated host ownership, and Keychain
credential storage before internet or hostile-LAN use.

Current release properties:

- **No unrequested background service access or telemetry.** The app has no analytics, account service, NAT traversal, relay, cloud multiplayer, or automatic update check. Network access follows a player-enabled feature: player-started LAN multiplayer; optional `/ai` or editor requests to local Ollama at `http://127.0.0.1:11434`; or the Reality Derived dialog/generator fetching map, search, elevation, and land-cover data from the providers identified by Arnis. Editor AI defaults to **Manual**, where only the dedicated action/hotkey sends a request; **Off** forbids editor model discovery and generation, while the separately persisted **On Idle** preference is an explicit opt-in that may send a cancellable request after typing pauses. The `elysium update` shell command separately runs `git pull` on your own checkout.
- **No accounts, no credentials, no personal data.** Elysium stores worlds, settings, and keybinds under `~/Library/Application Support/Elysium/`.
- **No elevated privileges.** It's an ad-hoc-signed app running in a normal user session.

That makes the realistic threat model: **malicious files that you load into the game, malformed output from local tools you explicitly connect to, and malformed traffic from peers on a local network you choose to join or host.**

## Attack surface

If you're auditing Elysium, these are the interesting places — all of them parse untrusted input:

| Surface | Where | Notes |
|---|---|---|
| Texture zips (bundled Faithful 64x and reviewed add-ons) | `Sources/Elysium/ResourcePacks.swift` | exact SHA-256 manifest; no-follow regular-file, single-link, bounded reads; atomic hash-verified managed-copy replacement; custom zip central-directory parser + raw-deflate via Apple's Compression framework; PNG decode via CoreGraphics. Bundled and user archives are treated as untrusted input |
| Save database and loose-save migration | `Sources/ElysiumStorage/StorageEngine.swift`, `Sources/ElysiumCore/Game/Saves.swift`, `Sources/ElysiumCore/Game/LegacySaveMigration.swift` | ElysiumStorage is the sole SQLite/SQL/handle owner behind a bounded named facade; the Core adapter retains domain codecs only. `VCK1` lengths are checked and out-of-range block ids clamp to air. Loose saves are traversed fd-relatively with no-follow opens, ownership/type/link checks, count/byte/resident caps, SHA-256 plus complete stat binding, an exclusive parent/source lease, replace-complete per-world import, a pre-rename durability barrier, checked namespace sync, restart/equivalence proof, and durable v2 provenance. Unmarked backup-only states stop before SQLite bootstrap with an 80-byte recovery-required record rather than inferring success or overwriting evolved rows. |
| Object templates | `Sources/ElysiumCore/Systems/Templates.swift`, `Sources/ElysiumCore/Game/Saves.swift`, `Sources/Elysium/WorldRenderer.swift`, `Sources/Elysium/ScreensM.swift` | Local construction templates stored in SQLite; Command-C copy names plus command/AI names are normalized and capped before save, the copy target comes from a fresh center-crosshair raycast, and clone traversal is limited to deterministic face- and edge-connected non-environment blocks while terrain, fluids, replaceables, and corner-only bridges are excluded. Template block counts, dimensions, volume, serialized size, block-entity count, block ids, and block-entity item stacks are validated before save/load/place/delete, and fresh clones strip inventory-bearing item slots plus deferred loot before save. New saves use a compact `PBT2` binary blob plus summary columns, legacy JSON rows remain size-capped, and browser/AI listing reads summaries instead of decoding every block in every saved template. `/place "name"` and Command-V browser placement arm a placement session and commit only on click; the browser previews only the currently selected validated template and deletes selected templates through a one-shot exact-name confirmation and normalized SQLite keys. A zero-row delete is reported as already gone; storage failures propagate across `SaveDB` without removing the visible row, and the UI exposes only a bounded generic failure message. Interactive placement validates the bounded object volume and capped support-fill footprint before clearing obstructions or filling foundation gaps; large placements are tick-sliced through a bounded job that preserves preflight and undo invariants; Command-Z restores one in-memory pre-placement snapshot for the last successful object placement without parsing persisted undo data; 3D placement and `/listTemplates` previews derive capped shape-box geometry from validated templates and upload large wireframes through Metal buffers instead of inline vertex bytes; AI template edits and generated pirate-ship templates go through the same validation path and never accept raw model-supplied coordinate arrays; screenshot smoke hooks only allowlist named UI screens |
| Map overlay | `Sources/ElysiumCore/Game/MapOverlay.swift`, `Sources/Elysium/MapOverlayM.swift`, `Sources/Elysium/HudM.swift`, `Sources/Elysium/ScreensM.swift` | Reads only loaded in-memory chunk height/top-block data; compact minimap size is limited to three fixed modes, maximum zoom-out is bounded to the currently loaded chunk extent, zoom-in bottoms out at about 100 blocks, and app-side drawing caps minimap/full-map sample resolution so a large loaded world cannot create unbounded UI work |
| Reality Derived generation | `Sources/Elysium/RealityDerivedM.swift`, `Sources/ElysiumCore/Gen/RealityDerivedImport.swift`, `Vendor/Arnis/`, `scripts/build-arnis-helper.sh` | Player-initiated WebKit map/search UI and a fixed bundled Rust helper; no shell or caller-selected executable/argument surface. The selected world tier, 250 km² physical cap, 250-million projected-column cap, scale, one-million imported-chunk cap, and deterministic terrain-transition expansion are bounded before persistence. The pinned `Cargo.lock` and source-commit receipt are checked during packaging. Generated directories are incomplete until a manifest-last rename; the importer uses no-follow regular-file reads, stable file-identity/length checks, canonical stream coordinate order, rectangular completeness, section and byte caps, strict RLE totals, bounded ground heights, registry-safe block mapping, and an exact-count streaming world-plus-chunks SQLite transaction. It retains only bounded manifest/palette/edge data plus one chunk payload; cancellation or any late validation/storage failure rolls back the complete transaction. Location rectangles and search terms are sent to Arnis's documented map providers, so players should not generate a sensitive location they do not want those providers to receive. |
| Settings/keybinds | `Sources/ElysiumCore/Game/Settings.swift` | plain JSON via `Codable`; loaded values are normalized back to the UI/runtime ranges before use |
| Local Ollama agent | `Sources/Elysium/OllamaAgent.swift`, `Sources/ElysiumCore/Systems/AIAgent.swift` | loopback-only HTTP to numeric `127.0.0.1:11434`, the single literal `scripts/security-scan.sh` allowlists (only in this one file); cloud-tagged Ollama models are filtered/rejected; `/api/chat` tool calls and the `/api/generate` structured-output fallback both decode to bounded `AIAgentAction` values; model output can only select whitelisted symbolic skills from `allAIAgentSkills` against registered Elysium content: `say`, `give_item`, cursor `place_block`, cursor `set_block_at_cursor`, cursor `break_block`, cursor `use_block`, bounded cursor `fill_region`, bounded player-relative `fill_hole`, bounded current-loaded-biome `rework_biome` for the fixed `rolling_hills_resource_rich` profile, normalized `set_time`, clear/rain/thunder `set_weather`, count-capped cursor `spawn_entity` for registered spawnable mobs, radius/count-capped non-player `remove_entities_nearby`, player-local `eat_selected_food`, `set_gamemode`, `heal_player`, `damage_player`, registered `apply_effect`, `clear_inventory`, bounded `add_xp`, durable current-position `set_spawnpoint`, world-global `set_difficulty`, existing-rule-only `set_gamerule`, surface-only `teleport_player`, saved-template block replacement, or bounded generated-template creation; raw coordinate arrays, arbitrary commands, unregistered ids, cloud endpoints, player removal, and unbounded block/entity mutation are not accepted. A request containing scripting vocabulary (`AIToolLoop.isScriptingRequest`) is routed instead to the bounded object-graph tool loop — see the row below; the two lanes never overlap in one request. |
| LAN multiplayer | `Sources/ElysiumCore/Net/LANMultiplayer.swift`, `Sources/ElysiumCore/Net/LANReplication.swift`, `Sources/ElysiumCore/Net/LANGameplayOrchestration.swift`, `Sources/Elysium/LANTransport.swift`, `Sources/Elysium/LANLobbyScreen.swift` | Bonjour service `_elysium-lan._tcp` plus TCP Direct Connect through Network.framework; `Info.plist` declares `NSLocalNetworkUsageDescription` and `NSBonjourServices`; frames carry `PBLN` magic, protocol version, message type, sequence, and bounded payload length before JSON decode; player names, join codes, direct hosts, chat text, template names, entity types, replicated arrays, world-state snapshots, chunk requests, chunk sections, inventory slots, inventory XP fields, block-entity slot snapshots, RPG intent ids, dimensions, death state, gameplay events, block cells, dropped-item payloads, and XP-orb amounts are normalized/capped or registry-validated; clients are accepted only after a join-code handshake; reconnect records are host-owned; full RPG state is host-owned and absent from normal high-frequency player snapshots; RPG quick slots are local repaired preferences and are not treated as host authority; remote players and mirrored world entities are transient runtime entities, not save entities; transient LAN client worlds suppress saved/worldgen entity adoption, request bounded host visible-neighborhood chunk snapshots instead of generating local chunks, and purge non-authoritative local entities; host-side remote-player proxies collect host-owned dropped items/XP into host-owned peer inventory snapshots, while clients cannot locally collect mirrored dropped entities; host-published block-entity item snapshots are accepted immediately only for loaded compatible chunks/blocks or held in a bounded deferred buffer until matching chunk/block state arrives, with slot ranges and registered item ids validated before mutation; client container-edit snapshots are proposals gated by host reach, permission, block compatibility, deterministic block-entity revision, item conservation, and valid crafting transforms, including up to two linked container block entities; remote `.useBlock` intents are accepted only for same-dimension, in-reach, non-iron doors, non-iron trapdoors, and fence gates, and still require build permission; RPG create/learn/prepare/attribute/select/cast/use intents are proposals, spell casts and active-skill uses require exact-next action sequence, and remote RPG world effects run through a nonpersistent host ghost player before host snapshots/deltas are broadcast; large host template place/undo mutations mark dirty chunk sections and send authoritative section snapshots instead of overflowing bounded block-delta queues; build/container/crafting/template/command/AI/creative/dimension/respawn actions are denied unless the host session grants the permission; a LAN client is refused outright for `/inspect`, `/objects`, `/events recent`, `/events list`, and every world-level `/script` subcommand (`trust`/`off`/`on`/`journal`/`undo-ai`/`list`/`show`) with no exceptions — `ScriptingCommands.lanClientRefusal(command:)` is the pure decision function and `CommandsM.swift`'s dispatch is the actual enforcement call site, checked before the command implementation ever runs; lan-client-parity (change 4) opens a narrow, permission-gated exception for the rest: `attr {set,define,remove}`, `script {attach,detach,run}`, `on`, `unsubscribe`, `events {define,remove,emit}`, and `/ai`/`/agent` are forwarded — never executed locally by the guest — as a new `scriptIntent` message kind (`LANMultiplayerMessageKind.scriptIntent`, its own tighter rate-limit bucket) the host re-validates against `ScriptingCommands.lanForwardableCommand` and a per-peer grant (`LANPeerPermissions.canScript` for the command family, the pre-existing but previously-unwired `.canUseAI` for `/ai`, both default `false`, both toggled only by a host-typed `/script trust <peer> [ai] [off]`) before dispatching through the *identical* `AttributeStore`/`ScriptStore`/`CustomEventStore`/`EventBus` executors a local command uses, with provenance `Provenance.Author.lan(peer:)`/`EventSource.lan(peerID:)` recorded instead of `.player` so every write's origin stays distinguishable from the host's own. `/ai` forwarding never lets a guest talk to Ollama directly — the prompt is relayed to the host's own bounded tool loop (`OllamaAgentService.runToolLoop`), sharing its one-in-flight-per-world gate, and only the model's final text is relayed back, as `.chat`, never a tool call or raw model output; custom event names, type-token fields, and summaries are mirrored as bounded authoring metadata only, never as guest authority; clients may not send raw save data, arbitrary command execution, authoritative RPG state, authoritative world state, or authoritative container contents |
| Embedded Lua script runtime | `Sources/CLua/**`, `Sources/ElysiumScript/**`, `Sources/ElysiumCore/Scripting/ScriptHostBindings.swift` | Untrusted script source text, chunk names, and every value crossing the C boundary are the trust boundary; the interpreter has no IO, network, process, dynamic-loading, `require`/`load`, `debug`, `coroutine`, or bytecode path, and text-only loading refuses any buffer beginning with the binary chunk signature. Every raise/yield/resume/pcall/load happens inside `CLua`'s own C (never in Swift), governed by a per-state `hostDepth` invariant so a raise can unwind only C frames between the raise site and the nearest protected entry, and the allocator never returns `NULL` while a Swift frame is on the stack. A fixed per-environment stdlib allowlist, frozen `__metatable`-locked API proxies, and closure-wrapped library functions (no light C function or light userdata reachable from scripts) isolate environments from each other and from the host. Instruction-slice, coroutine-total, global per-tick token-bucket, 64-suspended-per-script/1,024-per-world, consecutive-preemption, allocation-rate, memory-cap (16 MiB), pattern-matcher step, and per-library input/output caps bound every script; an already-running coroutine that trips a budget is recorded host-side, closed, and faulted regardless of Lua's own status, so `pcall` cannot revive it (a newly requested closure timer refused at suspension capacity is instead a normal catchable host-call error) — a fault is always per-script, never engine-fatal. Fixed downstream instruction reserves keep load, AI-reply, resumption, timer, and event lanes live under the production budget. Residual: C-side cost per instruction is bounded by the memory cap and the per-library input caps rather than by the instruction counter itself (a positional `table.insert`/`table.remove` shift or a hash-chain/`next` scan can still cost proportionally more per counted instruction than an arithmetic loop); this is host-only execution with no LAN-determinism exposure. The deterministic instruction scheduler is enforced; a separate wall-clock backstop remains future defense-in-depth for disproportionate but individually capped C-side operations. `ElysiumCore` adds the `ScriptHostBindings.swift` math/RNG seam. Scripts in saved worlds run with in-game authority inside this sandbox — never OS/files/network/processes; the world/dimension/block/entity/player verb surface, the trust gate, and the kill switch are covered by the row below. The AI now authors scripts too — see the "AI object graph tool loop" row: an AI-authored script runs inside the exact same sandbox as a player- or script-authored one, with no elevated capability |
| Script runtime application (records, lifecycle, kill switch, trust gate) | `Sources/ElysiumCore/Scripting/ScriptRecord.swift`, `ScriptStore.swift`, `ScriptRuntime.swift`, `ScriptRuntimeAPI.swift`, `ScriptTimers.swift`, `CustomEventDeclaration.swift`, `CustomEventStore.swift`, `Sources/Elysium/ScriptEditorUI/` | A script is attribute-store data (`AttributeEntry.script`) until the phase loads it. `ScriptStore.attach` enforces liveness/LAN authority, name/source-size/count, revision, and record/document caps; it deliberately does not own Lua compilation. The editor, AI `attach_script`, and script `h:attach` paths run `ScriptValidator` before storing, while the one-line `/script attach` storage command is compiled and linted by `ScriptRuntime` before execution in the next script phase. Thus malformed stored source may remain inspectable, but never executes. Every script — player-, AI-, or **script-authored** (`h:attach`/`h:define` plus object-scoped `h:declareEvent`/`h:undeclareEvent`/`h:on`/`h:onAttribute`/`h:emit`, with lifecycle mutations capped and provenance recorded as `.script(owner, name)` on every record or declaration it creates) — runs through the identical `ScriptStore`/`AttributeStore`/`CustomEventStore`/`EventBus` executors; nothing distinguishes a script's own mutations from a player's except the recorded provenance. Manual emission accepts only custom event names: standard events are engine-produced facts and every Lua, command, LAN, and AI funnel rejects attempts to forge them. Two independent switches gate attached execution and every ordinary command/AI/LAN one-off run, re-checked every phase, never cached: the persisted trust gate (`WorldRecord.scriptsEnabled` — false for every imported/migrated world until an explicit `/script trust`) and the `doScripts` gamerule kill switch (`/script off`\|`on`, takes effect the same tick, survives a hostile world since it never depends on the world's own scripts running to be reachable). `/script run` and the AI's `run_script` tool share one fully gated, capability-reduced, synchronous, non-persisted execution path (`ScriptRuntime.runEphemeral`): subscriptions, timers, named handlers, child attach/detach, `wait`, and `ai.*` are explicitly refused; its isolated transient RNG does not alter an attached script's durable stream. The native editor accepts direct multi-line input and keeps language analysis, nearby-object discovery, and optional Ollama proposals read-only. Its deterministic event picker and `ev` completion are target-aware and merge only compatible produced built-ins with the target's declared custom schemas. With a live local runtime, Check is a read-only dry run and Save only persists a validated record, which remains dormant while either switch is off. If no local runtime exists, Check, Save, and Run Once are unavailable because authoritative Lua validation cannot be performed. Explicit local editor Run Once uses the same capability-reduced implementation but may bypass only persisted world trust for the visible draft; it still obeys `doScripts`, does not save or attach the draft, does not flip trust, and does not load attached scripts, but its allowed one-off verbs can mutate live game state and those mutations may persist with the world. A separate confirmation warns that changing trust and/or the kill switch may start all enabled attached scripts. Opening, editing, Save, Check, and Run never change either gate. These exceptions are unavailable to commands, AI tools, and LAN forwarding, which remain fully gated. Every available Save/Check/Run Once still crosses the existing hygiene/validator/executor boundary. Editor Ollama completion remains Manual by default and is a separate text-only, no-tools service and cannot attach, execute, emit, query hidden state, or mutate the world. Script lifecycle churn is charged before mutation and capped at 2 attach/detach operations per script and 32 per world per tick. Attribute and script entries share a name namespace but their mutation APIs refuse cross-kind replacement without changing revision or lifecycle state; callers must explicitly remove/detach first. Persisted hydration and unload feed exact refs into a canonical deduplicating queue; one phase reconciles at most 64 refs and starts at most 64 definitions, retaining every suffix and later-generation edit without any periodic whole-world scan. |
| AI object graph tool loop (query/mutation tools, validation+dry-run gate, journal/undo, script broker) | `Sources/ElysiumCore/Scripting/AI/*.swift`, `Sources/Elysium/OllamaAgent.swift`, `Sources/Elysium/AIScriptBroker.swift` | The AI can now see and mutate the object graph, through the same trust boundary as everything above it, never a shortcut around it. **Model output stays untrusted data reduced to a fixed tool surface** (`AIToolLoop.allDefinitions`, 10 read-only query tools + 10 mutation tools) — the model can never send Swift code, SQL, shell text, or a raw file path; every tool call is a JSON object matched against a declared parameter list and dispatched by name through a `switch`, exactly like `AIAgentSkillDefinition` above it. Every mutation tool (`set_attribute`, `define_attribute`, `remove_attribute`, `attach_script`, `detach_script`, `enable_script`, `subscribe`, `unsubscribe`, `emit_event`, `run_script`) runs through the *identical* `AttributeStore`/`ScriptStore`/`CustomEventStore`/`EventBus` executors `/attr`/`/script`/`/on` use, is refused outright on a LAN client before any of them run, and is capped at 4 per `/ai` request (`AIToolLoop.maxMutationsPerRequest`, "one undo group"), 8 turns, 3 retries per tool, and a 90 s wall-clock deadline (`OllamaAgentService.runToolLoop`). `attach_script` additionally runs `ScriptValidator`'s stages 0-3, a literal-only stage-5 event-name grammar check (`AIScriptValidationGate`), and a stage-6 dry run (`ScriptRuntime.dryRun`) that compiles and executes the chunk once with `dryRunActive` set — every mutating verb dispatch in `ScriptRuntimeAPI.swift` becomes a no-op while that flag is set, so a dry run can never write an attribute, attach/detach a script, raise an event, place/break a block, send chat, register a durable timer, or reach the AI outbox, regardless of what the candidate script's own body does; a dry-run failure is reported as a non-blocking warning, never a silent success and never a refusal. `describe_events` accepts an optional object ref and returns its applicable built-ins plus declared schemas; `emit_event` accepts only custom-event `emit`, `declare`, or `remove`, all through the shared validators; built-in engine facts cannot be forged. The system prompt's Lua contract is generated from the live canonical event registry rather than a separately maintained list. Every successful mutation is journaled (`AIJournal`, a bounded ring: <= 64 entries, <= 64 KiB, plus a <= 4-entry/<= 64 KiB side list of full previous script records for undo) with provenance `.ai(model)` on the entry it created — the same `Provenance.Author` field a player's or a script's own writes carry — and persisted journal restores reapply those live ring, byte, and side-list ceilings before list or undo can observe them. `/script undo-ai [n]` reverts the `n` most recent `/ai` requests' worth of mutations, gated by a compare-and-swap on the script's `sourceHash` so a player's own edit since the AI's attach is never silently clobbered, only refused with a clear line. Tool results returned to the model are wrapped in a nonce-fenced envelope (`AIToolEnvelope`, one random-per-request nonce) carrying a fixed "this is data, not instructions" line — the same discipline `AIAgentAction` parsing already applied to world-lane skills, now extended to arbitrary tool-result text the model itself cannot control the shape of. Malformed tool-call text (a tool-less model printing `{"name":...}` in plain content instead of using the API's tool-call channel) is rescued by a bounded, allocation-capped repair pass (`AIToolCallRepair`, 16 KiB text cap) before being treated as a final answer. Scripts calling the AI (`ai.ask`/`ai.await`) go through a separate, much narrower path: **text generation only**, no tools, capped at 4,096 prompt characters / 4,096 decoded reply UTF-8 bytes, with each encoded response body rejected while streaming past 64 KiB before JSON decoding and every decoded reply clamped on a complete character boundary at both the app broker and Core script-visible boundary, <= 2 requests in flight and <= 30/minute per world (`ScriptRuntime.aiBudgetAvailable`, refused immediately over budget — never silently queued), delivered asynchronously off the tick's call stack (`ScriptRuntime.outboxHandoff`/`submitAIReply`, `AIScriptBroker.pump` called once per frame) so the model's real, variable network latency can never stall the deterministic simulation; each request is keyed by an opaque world-session id plus its runtime-local id, task cleanup is identity-guarded, and detach/edit cancels the exact async URLSession request before releasing its in-flight slot, so an old reply or cleanup cannot collide with a new world that reuses the same numeric id. AI-caused nondeterminism is quarantined behind this whole boundary by construction: nothing the model does ever touches game state except through the same provenance-stamped, journaled, budgeted paths a player's own commands and a script's own verbs already use. |
| Object attributes, event declarations, and scripting commands | `Sources/ElysiumCore/Scripting/ObjectRef.swift`, `ObjectGraph.swift`, `AttrValueCodec.swift`, `ObjectRecord.swift`, `AttributeRegistry.swift`, `BuiltInAttributes.swift`, `BlockStateCodec.swift`, `AttributeStore.swift`, `CustomEventDeclaration.swift`, `CustomEventStore.swift`, `ScriptingCommands.swift`, `Sources/Elysium/CommandsM.swift` | Reachable through `/attr`, `/inspect`, `/objects`, the shipped Lua handle API, the read-only editor World Objects snapshot, and world persistence. `ObjectRef` parses a strict canonical grammar only (`entity:<uid>`, `block:<dim>:x,y,z`, `self`/`looking` aliases); `AttrValue` (`= ScriptValue`) crosses no C boundary here and is validated by a hand-rolled canonical-JSON codec, never `JSONSerialization` (which cannot preserve the `Int64`/`Double` distinction or reject duplicate keys). `AttributeStore` is the sole mutation executor and enforces per-entry, per-value, and per-record size caps, a smaller cap for the world-scoped attribute bag, and a monotonic per-record revision counter with overflow-safe, refuse-on-overflow arithmetic; `AttributeRegistry` gates every built-in attribute's mutability and applicability before a write reaches `BuiltInAttributes`/`BlockStateCodec`. Custom event declarations persist as a separate object-scoped map, share object/chunk/world storage caps, and are limited to 16 events per object, 32 typed fields per event, and a 256-byte summary. Field tokens are `any|boolean|integer|number|string|object|list|map` with optional `?`; a matching declaration makes payload validation strict, while undeclared custom names remain legal for compatibility. Lua map keys are bounded symmetrically in both marshaling directions; untrusted custom emission reapplies the persistent value-shape rules, including the 256-byte map-key ceiling. EventBus then allocation-bounded-sizes every engine or untrusted payload before retention: <= 16 KiB/event, <= 4 MiB across pending entries/current cursor/one deferred diagnostic, and <= 512 KiB across the <= 128-entry recent ring. Coalescing is transactional and every pending-to-cursor-to-deferred transfer preserves exactly one byte charge. Any script may operate on any live handle, but all object-first methods still cross these liveness, cap, mutability, provenance, and host-authority checks. The editor enumerates only `ObjectGraph` results and inserts canonical refs, never display names or guessed identity. A LAN client is refused outright for direct local execution (see the LAN multiplayer row); execution of every mutation stays host-only regardless — a `canScript`-granted guest reaches these same executors only through a validated `scriptIntent`, never by calling them itself. `ObjectRef.lanPlayer` (`player:lan:<peerID>`) resolves live only on the host, for a currently-connected peer, and deliberately reuses the existing `.entity` `LiveObject` case (`LANRemotePlayerEntity` already is an `Entity` subclass) rather than adding a new exhaustive-switch surface; a guest never resolves another peer's (or its own) `player:lan:*` object locally at all, reading only the replicated read-only attribute/script/event metadata mirror. Entity uid persistence (`Entity.id`, `EntityRegistry.loadEntity`) accepts a saved id only as a genuine in-range integer-typed JSON number and reserves a durable hi/lo id block ahead of minting so a crash between mint and flush can never hand out a uid a prior session already used; persisted revision/timestamp fields are parsed by a strict integer-token parser and clamped, never trusted at face value. |
| Vendored interpreter provenance | `scripts/clua/rederive.sh`, `scripts/clua/elysium.patch`, `scripts/clua/upstream-manifest.json`, `scripts/fdlibm-reference/` | `Sources/CLua` is Lua 5.4.8 vendored from an SHA-256-and-byte-size-pinned upstream tarball (`https://www.lua.org/ftp/lua-5.4.8.tar.gz`) plus a checked-in, eleven-file Elysium patch that pins determinism (hash seed, ordinal key hashing, locale, `FP_CONTRACT OFF`) and removes the unverified bytecode loader (`lundump.c` stubbed). `scripts/clua/rederive.sh` re-fetches or accepts a local tarball over `curl --fail --proto '=https' --tlsv1.2 --max-redirs 0`, verifies the hash and byte size before extraction, and fails closed on any byte drift between tarball-plus-patch and the committed tree; `Tests/ElysiumScriptTests/CLuaSourceTests.swift` reverse-applies the patch against the manifest's per-file hashes with no network access, so provenance is checked on every `swift test` run, not only when the vendored copy changes. The same pattern (pinned hash, checked-in generator, hermetic reverse check) covers the independent fdlibm reference used to pin `detExp`/`detLog`/`detPow`. There is no automated upstream-CVE feed for the pinned Lua release: because the interpreter is statically vendored rather than tracked as a live dependency, a maintainer must periodically check lua.org's release notes against the pinned 5.4.8 and, if a security-relevant fix is published upstream, re-run `rederive.sh` against the newer tarball and hand-port the Elysium patch onto it — this is a manual watch procedure, not a scripted gate |

For script-originated AI, awaited reply ownership is transactional: a reply is retained with its
request mode, in-flight slot, and exact suspended coroutine until instruction credit permits that
coroutine to resume. Request-id suffix ordering prevents later replies from overtaking a deferred
waiter, while edit/detach cancellation removes all four pieces together.

RPG local preferences use a closed ElysiumStorage component capability with exact schema markers, row/count/aggregate byte caps, strict `PBLQS1` decoding, domain-separated SHA-256 verification, immutable legacy-origin binding, stale-CAS rejection, and atomic deletion with the owning world. LAN-shaped identities cannot enter this local façade. The LAN-v6 client checkpoint schema and aggregate storage façade are compiled, but component bootstrap remains test-only and the production accessor is verify-only. ElysiumCore has no client checkpoint codec, candidate builder, `SaveDB` entry point, or façade acquisition: those remain blocked on the reviewed Phase-2.5 owner/request/send-state/disposition codecs and transition matrix, so protocol 5 cannot persist or publish pseudo-validated authority bytes. Host owner rows expose neither a writer nor a bootstrap. Component bootstrap accepts only complete legal prefixes and schema audit rejects partial components, alternate DDL, unexpected indexes, views, and user triggers.

Saved-world batch deletion is fail-closed against corrupted or racing local database state. Checked snapshots cap IDs at 256 UTF-8 bytes, rows at 4,096, aggregate raw row data at 67,108,864 bytes, request/receipt envelopes at 1,310,720 bytes, chunk rows at 1,048,576, and statement work at 53,248 through one overflow-checking production accumulator. Domain-separated SHA-256 binds exact IDs, JSON bytes, `lastPlayed` bits, collection order, requests, and receipts. The single transaction pre-counts and post-proves world, chunk, player, advancement, RPG preference, and RPG migration scopes; prepared delete statements are reset and clear-bound between IDs. Ambiguous continuation is a request-bound read-only authority: it never reissues deletion, exact-post proof retires only the selected RPG omission IDs, and exact-pre or unresolved proof preserves them and the global maintenance lease. Recovery uses a verify-only RPG-local schema audit; missing, partial, or corrupt component objects remain absent/corrupt and terminal rather than authorizing bootstrap or mutation. Compiler-enforced main-actor admission guards world transitions and LAN hosting. UI and Accessibility never expose raw IDs, digests, SQL, paths, corrupt values, underlying errors, control characters, Unicode format controls, or bidi direction controls, and revalidate screen generation, key-window membership, enabled state, and action identity before Press.

The RPG UI harness is detected before ordinary app construction and fails closed when its exact six-key
`ELYSIUM_` allowlist is mixed with any shipping automation key. Environment count, aggregate bytes,
key/value bytes, selector grammar, registries, relationships, viewport, appearance, authority, and
screenshot basename/frame values are bounded before AppKit setup. Fixture construction is pure and
has no ordinary dependency factory. Optional screenshots use `openat` beneath held no-follow
directory descriptors, require an owner-only harness directory, create the basename with
`O_EXCL|O_NOFOLLOW` and mode `0600`, and clean an incomplete reservation on termination. Semantic
output is sorted/bounded and excludes player, owner, credential, path, and raw notice payload data.

Nether World adds no external input or authority surface. Its preset id passes through the existing closed normalization list, gateway placement is a bounded deterministic function of seed and chunk coordinates, and the starter grant executes only when no player record exists; persisted or malformed player data cannot replay that grant.

Hardening that already exists: chunk-blob decoding validates section lengths and clamps corrupted block ids to air; RPG player-data loading repairs attributes, class/path ids, skill ranks, prepared skills, spells, selected action tokens, quick-slot tokens, fatigue, cooldowns, and upkeep records against static registries; LAN replication keeps clients intent-only, caps every replicated collection, drops malformed world-state dimensions, malformed chunk-request dimensions/radii, section payloads, registry-invalid cells, unknown entity types, invalid dropped-item ids/counts, invalid inventory slot/item/XP fields, block-entity slot/item payloads, incompatible block-entity snapshots, client-authored RPG snapshots in normal player state, stale/future-skipping RPG cast/use sequences, remote block/use intents from the wrong dimension or outside reach, stale container edit revisions, duplicate linked container targets, non-conservative container item transitions, and invalid XP-orb amounts, bounds and replays host block/block-entity deltas that arrive before their chunks, applies host world mutations only on the main game thread, captures host runtime block mutations through a bounded coalescing log, requeues dirty block entities when authoritative sends cannot be encoded or delivered, uses dirty chunk-section snapshots for template mutations too large for block-delta replication, clamps client-sent player state through host permissions, preserves reconnect state in host-owned peer records, removes dead/disconnected/different-dimension remote player entities, opens accepted title-screen clients into transient LAN worlds at the locally remembered per-host-world client resume position when available or at the advertised spawn height otherwise, refuses to save those transient client worlds as local singleplayer worlds, persists only the local LAN client's resume player snapshot for the matching host world id/seed, never persists remote runtime player entities or mirrored world entities as chunk entities, suppresses saved/worldgen entity adoption and local chunk generation in transient LAN client worlds, requests center-first visible host chunk neighborhoods under the replication frame cap, purges non-authoritative client-only entities, skips normal physics/AI/pickup ticks for mirrored world entities, applies host inventory snapshots only to the local player id that owns them, updates replicated block entities in place so already-open mirrored screens do not retain stale item arrays, submits mirrored container/crafting screen edits as revision-gated host-validated intents, and limits remote `.useBlock` to deterministic openable-block deltas instead of running the full local screen/item/sleep path; template cloning and placement cap volume/span/block count/block-entity count/legacy JSON size/binary size, traverse only cloneable face/edge neighbors, strip fresh-clone container item payloads and deferred loot, preflight all destination cells before mutation, cap prepared-placement support fill depth, tick-slice large interactive placements, snapshot placement undo state only from validated in-memory mutations, and bound both `/place` wireframe preview work and `/listTemplates` preview work by box count after template validation; AI terrain-leveling fills never accept model coordinates, require a dirt-like rim in front of the player, write only registered blocks into loaded chunks, and cap search distance, horizontal radius, depth, and total block count; AI biome rework never accepts model coordinates or raw block arrays, operates only on the loaded contiguous biome patch around the player, writes terrain through `World.setBlock`, preserves non-natural/block-entity columns, caps terrain/resource writes, and adds resources only by replacing existing natural underground stone/deepslate with registered ore blocks; AI template edits resolve source and destination blocks through the registered block registry, reset changed-cell metadata, drop block entities attached to changed cells, and re-encode templates before saving; the live map overlay bounds zoom/pan math to finite loaded chunk extents and caps per-frame UI sampling; crafting-table access to nearby storage is radius-bound and still withdraws concrete recipe ingredients before normal grid consumption; player-data loading repairs array sizes and drops out-of-range item ids, and spawn-point updates from beds/respawn anchors flush through the existing player save record immediately; SQLite errors are surfaced and failed writes retried rather than ignored; the zip reader never writes outside its own buffers (it extracts to memory, not to disk paths from the archive), caps archive/file sizes, rejects path-traversal entries, and skips symlinked folder-pack files.

`unload` is not an EventBus delivery surface. A live module may register the reserved
`register("unload", fn)` finalizer, which runs synchronously during edit/disable/detach, object
unload, or session shutdown with no `ev`. Teardown enables only final custom-attribute writes on
live handles; it refuses yielding, timers, AI, event/subscription/registration changes, declaration
or script mutation, world or built-in mutation, RNG, and player/audio/particle output. The callback
is instruction-bounded, its failure cannot stop environment destruction, and a stale callback cannot
overwrite a replacement script's diagnostic.

Furnace output control is likewise an attached-script capability, not ambient persisted block
state. `self:setFurnaceOutput(item)` accepts only a registered item on the calling script's own
loaded furnace-family block, admits one current controller per furnace, performs validation without
registration during Check, and is refused by Run Once and unload. The override exists only in
`ScriptRuntime`; the furnace ticker rechecks both execution gates and the controller's current
definition, so editing, disabling, detaching, faulting, unloading, or ending the session removes or
immediately masks that authority. Recipe input and XP remain engine-owned, and `furnace.smeltCompleted` is
an engine-produced fact that manual emit paths cannot forge.

`block.toolStrike` is emitted only on a new real-tool mining-target transition, not continuously while
the same target remains held. Its payload is exactly `by`, `item`, `blockName`, `face`, `toolType`,
`tier`, and `instant`; first contact with an unbreakable block is still an event, with `instant=false`.
Remote delivery is intent-only and event-only. The host rejects unknown,
disconnected, dead, unauthorized, wrong-dimension, out-of-reach, stale-block, invalid-face,
mismatched-slot, non-tool, and exhausted-tool claims against its current peer/world/inventory
records. A positive per-connection high-water sequence ignores duplicate or older strikes;
authorized semantic failures consume their sequence, gaps remain valid after packet loss, reconnect
resets the host epoch, and the bounded client counter fails closed instead of wrapping.
Guest-provided names, tool metadata, tier, and instant-break status never enter the emitted payload.

NPC barter treats saves, merchant state, and receipts as hostile inputs. Decoding caps payload and offer counts and validates registry ids, stack metadata/counts, stable ids, tiers, uses, XP, timers, and duplicates before secondary materialization or registry indexing. Opaque one-shot receipts bind runtime object identity, world/player/merchant attachment, reach/line of sight, merchant revision, offer digest, inventory digest, and a bounded nonce set; replay, stale state, overflow, and LAN-client calls fail before mutation. Payment and result insertion are simulated on a detached inventory, then inventory and merchant post-state publish before sound, particles, or advancement callbacks.

Worldgen planning never trusts partially generated target state as cross-chunk authority. The exact bounded base-terrain oracle and context-complete single-flight caches prevent seed/preset aliasing and unbounded duplicate work; candidate and piece caps constrain adversarial coordinates/settings. Village and dungeon planners validate before detached writes, dungeon block entities are checked against their owning cells before commit, and underwater selection is region-budgeted. Existing saved full chunks remain authoritative and receive no migration or automatic seam repair.

Checked player-row persistence exposes only absent/exact-digest expectations and conflict/committed
results. Its private authorizer reads the exact world parent and player row, permits only player
INSERT or JSON-only UPDATE, and returns redacted Core errors. Candidate encoding occurs before rank
12; stale writers, missing parents, world deletion, hostile row classes, and transaction faults
cannot turn the CAS into an unconditional overwrite.

Host runtime allocation/cap testing remains blocked on the future reviewed host parent schema and
coherent host façade; no dormant test façade widens that boundary.

Local verification scripts:

- `./scripts/security-scan.sh` checks source for unapproved network/process/dynamic-loading APIs and obvious secret material. Its bounded local-release checks reject production fixture/fault selectors, verify release entry-point ownership and modes, validate the exhaustive source-snapshot contract, and reject retired release-authority surfaces. It also runs the lexical SQLite boundary scanner, compiling its bypass fixture, rejecting SQLite/SQL/storage-capability escapes, comparing the SwiftPM source inventory and dependency direction, and byte-pinning ElysiumStorage's package/SPI symbol graph plus the exact two-file Core capability inventory. The same boundary scanner enforces the sole-Lua-owner rule: `CLua` may be imported only by `Sources/ElysiumScript/`, no `lua_`/`luaL_`/`LUA_`-prefixed identifier (or any identifier/import containing `CLua`) may appear outside it, the raising-API denylist and `luaopen_`/`lua_topointer` are banned inside it, and `OpaquePointer` for a Lua state is permitted only in `Sources/ElysiumScript/LuaState.swift` (alongside the pre-existing `StorageEngine.swift` SQLite pattern); `scripts/security-scan.sh` separately bans `luaopen_(io|os|package|debug|coroutine)`, `luaL_openlibs`, and `setlocale` call sites anywhere under `Sources/`. Network APIs are allowed only in the loopback Ollama client and the LAN transport adapter.
- `./scripts/verify-pack-assets.sh` verifies exact hashes, ZIP integrity, required texture/license/credit entries, and case-folded path uniqueness for Faithful 64x, Ore Borders 64x, and Static Lanterns.
- `./scripts/security-check-binary.sh /Applications/Elysium.app` verifies the app signature, bundle metadata, linked library paths, and network-related binary symbols/strings. It allows only the local Ollama URL, requires LAN privacy/Bonjour declarations before accepting Network.framework symbols, and rejects other URL literals.
- `./scripts/pipeline.sh` runs the full source-security, warning-free build, release-surface/binary, XCTest, golden, package, packaged-AppKit, install, and installed identity/codesign pipeline.

The unattended AppKit gate is deliberately fail closed. It requires the current unlocked console user plus
Accessibility/Input Monitoring trust, a regular hash-pinned release executable, strict ad-hoc
signing and resource sealing, and exact launched bundle/PID identity. It has zero general-pasteboard
or Paste activity. Deadlines, signal cleanup, redacted diagnostics, and release rehashing prevent an
adapter-only or stale installed binary from satisfying it. Production Paste adapter tests cover
readiness/owner/bounds/race behavior.

The pipeline binds all tracked and nonignored-untracked regular inputs at entry and revalidates them
after every stage. It rejects persistent additions, removals, replacements, mode changes, unsafe
links/types, and boundary-visible races. The current console user and workspace are trusted: an
exact same-user mutate-and-restore wholly between boundary scans and malicious local build plugins
are explicitly out of scope. The pipeline therefore does not claim concurrent-tamper resistance.
Executable SHA continuity remains mandatory from warning-free build through signed package,
packaged AppKit execution, installation, and final independent identity/signature verification.

## Reporting a vulnerability

If you find a way for a crafted save file or texture archive to do anything beyond crashing the game (memory corruption, code execution, file writes outside the support directory), please report it privately:

- **Email:** briangaoo2@gmail.com — subject line starting with `[elysium security]`
- Include: macOS version, a minimal reproducing file if possible, and what you observed.

Plain crashes / hangs from malformed files are ordinary bugs — file those as [regular GitHub issues](https://github.com/mweingartner/elysium/issues) with the offending file attached (the README lists what else to include). This is a beta; reports of every kind are incredibly welcome.

You can expect an acknowledgment within a few days. There's no bug bounty; you'll get credit in the changelog and my genuine thanks.

## Supported versions

Only the latest release is supported. There's no backporting; the fix ships in the next version.
