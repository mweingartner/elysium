#!/bin/sh
set -eu
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"
# mpd pre-commit gate: fast staged secret and policy checks.
mpd check --staged --quiet || {
    echo "pre-commit blocked: mpd staged checks failed." >&2
    exit 1
}
