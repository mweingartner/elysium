let actionLabel = "Select "
let authorityHelp = "Character changes are disabled while Elysium restores the host state."
let progressionSource = "Skills from this path remain visible."
let unrelatedArithmetic = 20 + 22

func unrelatedLiteralSummary() -> String {
    "\(actionLabel): \(authorityHelp) \(progressionSource) \(unrelatedArithmetic)"
}
