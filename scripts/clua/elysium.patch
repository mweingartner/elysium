diff -ruN --exclude=elysium_internal.h --exclude=elysium_sandbox.c --exclude=elysium_shim.c --exclude=elysium_shim.h --exclude=module.modulemap a/include/lauxlib.h b/include/lauxlib.h
--- a/include/lauxlib.h	2026-08-22 13:16:03
+++ b/include/lauxlib.h	2026-08-22 13:53:49
@@ -69,8 +69,8 @@
 
 LUALIB_API int   (luaL_newmetatable) (lua_State *L, const char *tname);
 LUALIB_API void  (luaL_setmetatable) (lua_State *L, const char *tname);
-LUALIB_API void *(luaL_testudata) (lua_State *L, int ud, const char *tname);
-LUALIB_API void *(luaL_checkudata) (lua_State *L, int ud, const char *tname);
+/* Elysium: the two userdata-metatable-check helpers upstream declared here are
+** removed (see lauxlib.c). */
 
 LUALIB_API void (luaL_where) (lua_State *L, int lvl);
 LUALIB_API int (luaL_error) (lua_State *L, const char *fmt, ...);
diff -ruN --exclude=elysium_internal.h --exclude=elysium_sandbox.c --exclude=elysium_shim.c --exclude=elysium_shim.h --exclude=module.modulemap a/include/luaconf.h b/include/luaconf.h
--- a/include/luaconf.h	2026-08-22 13:16:03
+++ b/include/luaconf.h	2026-08-22 13:53:49
@@ -792,10 +792,43 @@
 /*
 ** Local configuration. You can use this space to add your redefinitions
 ** without modifying the main part of the file.
+**
+** Elysium determinism and boundary patches (scripts/clua/elysium.patch):
+**   - luai_makeseed: fixed hash seed, not ASLR/time derived (script-determinism spec).
+**   - l_randomizePivot: fixed (non-random) table.sort pivot.
+**   - lua_getlocaledecpoint: '.' independent of the process locale (must #undef the
+**     upstream definition above, since this file defines it unconditionally).
+**   - elysium_numpow / luai_numpow: '^' and constant folding route through the host's
+**     ScriptMath table (elysium_shim.c), except the x*x fast path for exponent 2.
+**   - ELYSIUM_MATCH_STEPS: lstrlib.c pattern-matcher step budget (C22).
+**   - ELYSIUM_MAX_STRING: lvm.c luaV_concat concatenation-result cap (C22).
+**   - LUA_COMPAT_MATHLIB is left undefined (belt and braces: it is only ever defined
+**     under LUA_COMPAT_5_3, which this build never defines).
+**   - FP_CONTRACT OFF: no fused multiply-add contraction, since -ffp-contract=off
+**     cannot be passed as an unsafe flag under SwiftPM.
 */
 
+#undef lua_getlocaledecpoint
+#define lua_getlocaledecpoint()	'.'
 
+#define luai_makeseed(L)	((void)(L), 0x454C5953u)
 
+#define l_randomizePivot()	0u
+
+/* 'lua_State'/'lua_Number' are not yet declared this early in lua.h (luaconf.h is
+** included before their typedefs) — forward-declare the struct and use LUA_NUMBER
+** (already #defined above) directly instead of the not-yet-existing 'lua_Number'.
+*/
+struct lua_State;
+extern LUA_NUMBER elysium_numpow (struct lua_State *L, LUA_NUMBER a, LUA_NUMBER b);
+#define luai_numpow(L,a,b)	((b == 2) ? (a)*(a) : elysium_numpow(L,a,b))
+
+#define ELYSIUM_MATCH_STEPS	100000
+#define ELYSIUM_MAX_STRING	262144
+
+#undef LUA_COMPAT_MATHLIB
+
+#pragma STDC FP_CONTRACT OFF
 
 
 #endif
diff -ruN --exclude=elysium_internal.h --exclude=elysium_sandbox.c --exclude=elysium_shim.c --exclude=elysium_shim.h --exclude=module.modulemap a/lauxlib.c b/lauxlib.c
--- a/lauxlib.c	2026-08-22 13:16:03
+++ b/lauxlib.c	2026-08-22 13:53:49
@@ -330,27 +330,12 @@
 }
 
 
-LUALIB_API void *luaL_testudata (lua_State *L, int ud, const char *tname) {
-  void *p = lua_touserdata(L, ud);
-  if (p != NULL) {  /* value is a userdata? */
-    if (lua_getmetatable(L, ud)) {  /* does it have a metatable? */
-      luaL_getmetatable(L, tname);  /* get correct metatable */
-      if (!lua_rawequal(L, -1, -2))  /* not the same? */
-        p = NULL;  /* value is a userdata with wrong metatable */
-      lua_pop(L, 2);  /* remove both metatables */
-      return p;
-    }
-  }
-  return NULL;  /* value is not a userdata with a metatable */
-}
+/* Elysium: the two userdata-metatable-check helpers upstream provided here (whose
+** names embed a release-surface-denylisted "_test" substring) are removed. The
+** shim checks handle metatables itself with lua_rawequal against a registry entry
+** instead; these two are the only vendored casualties of that rule.
+*/
 
-
-LUALIB_API void *luaL_checkudata (lua_State *L, int ud, const char *tname) {
-  void *p = luaL_testudata(L, ud, tname);
-  luaL_argexpected(L, p != NULL, ud, tname);
-  return p;
-}
-
 /* }====================================================== */
 
 
@@ -920,10 +905,14 @@
         lua_pushliteral(L, "nil");
         break;
       default: {
+        /* Elysium: type name (or '__name') only, never an address — no lua_topointer
+        ** call, so tostring()/print() cannot leak heap addresses to scripts
+        ** (script-determinism spec: address-free output).
+        */
         int tt = luaL_getmetafield(L, idx, "__name");  /* try name */
         const char *kind = (tt == LUA_TSTRING) ? lua_tostring(L, -1) :
                                                  luaL_typename(L, idx);
-        lua_pushfstring(L, "%s: %p", kind, lua_topointer(L, idx));
+        lua_pushfstring(L, "%s", kind);
         if (tt != LUA_TNIL)
           lua_remove(L, -2);  /* remove '__name' */
         break;
diff -ruN --exclude=elysium_internal.h --exclude=elysium_sandbox.c --exclude=elysium_shim.c --exclude=elysium_shim.h --exclude=module.modulemap a/lgc.c b/lgc.c
--- a/lgc.c	2026-08-22 13:16:03
+++ b/lgc.c	2026-08-22 13:53:49
@@ -261,6 +261,7 @@
   GCObject *o = cast(GCObject *, p + offset);
   o->marked = luaC_white(g);
   o->tt = tt;
+  o->ordinal = g->nextOrdinal++;  /* Elysium: creation-order key for mainpositionTV */
   o->next = g->allgc;
   g->allgc = o;
   return o;
diff -ruN --exclude=elysium_internal.h --exclude=elysium_sandbox.c --exclude=elysium_shim.c --exclude=elysium_shim.h --exclude=module.modulemap a/lobject.h b/lobject.h
--- a/lobject.h	2026-08-22 13:16:03
+++ b/lobject.h	2026-08-22 13:53:49
@@ -284,8 +284,15 @@
 /*
 ** Common Header for all collectable objects (in macro form, to be
 ** included in other objects)
+**
+** Elysium: 'ordinal' is a per-object creation sequence number (assigned by
+** 'luaC_newobjdt' in lgc.c) used by 'mainpositionTV' in ltable.c to hash tables,
+** closures, threads and userdata by creation order instead of by pointer, so that
+** iteration order never depends on heap layout (script-determinism spec). It wraps
+** after 2^32 objects in one state, which the 16 MiB memory cap makes unreachable in
+** one session.
 */
-#define CommonHeader	struct GCObject *next; lu_byte tt; lu_byte marked
+#define CommonHeader	struct GCObject *next; lu_byte tt; lu_byte marked; l_uint32 ordinal
 
 
 /* Common type for all collectable objects */
diff -ruN --exclude=elysium_internal.h --exclude=elysium_sandbox.c --exclude=elysium_shim.c --exclude=elysium_shim.h --exclude=module.modulemap a/lstate.c b/lstate.c
--- a/lstate.c	2026-08-22 13:16:03
+++ b/lstate.c	2026-08-22 13:53:49
@@ -380,6 +380,8 @@
   g->warnf = NULL;
   g->ud_warn = NULL;
   g->mainthread = L;
+  L->ordinal = 0;  /* Elysium: main thread is object 0 */
+  g->nextOrdinal = 1;  /* Elysium: set before f_luaopen creates further objects */
   g->seed = luai_makeseed(L);
   g->gcstp = GCSTPGC;  /* no GC while building state */
   g->strt.size = g->strt.nuse = 0;
diff -ruN --exclude=elysium_internal.h --exclude=elysium_sandbox.c --exclude=elysium_shim.c --exclude=elysium_shim.h --exclude=module.modulemap a/lstate.h b/lstate.h
--- a/lstate.h	2026-08-22 13:16:03
+++ b/lstate.h	2026-08-22 13:53:49
@@ -262,6 +262,7 @@
   TValue l_registry;
   TValue nilvalue;  /* a nil value */
   unsigned int seed;  /* randomized seed for hashes */
+  l_uint32 nextOrdinal;  /* Elysium: next CommonHeader.ordinal to assign (lgc.c) */
   lu_byte currentwhite;
   lu_byte gcstate;  /* state of garbage collector */
   lu_byte gckind;  /* kind of GC running */
diff -ruN --exclude=elysium_internal.h --exclude=elysium_sandbox.c --exclude=elysium_shim.c --exclude=elysium_shim.h --exclude=module.modulemap a/lstrlib.c b/lstrlib.c
--- a/lstrlib.c	2026-08-22 13:16:03
+++ b/lstrlib.c	2026-08-22 13:53:49
@@ -366,6 +366,7 @@
     const char *init;
     ptrdiff_t len;
   } capture[LUA_MAXCAPTURES];
+  unsigned long steps;  /* Elysium: matcher step budget for this library call (C22) */
 } MatchState;
 
 
@@ -379,6 +380,19 @@
 #endif
 
 
+/* Elysium: charge one matcher step, raising "pattern too complex" at zero. Called
+** at every 'match' entry and at every iteration of the two non-recursive expansion
+** loops in 'max_expand' and the loop in 'min_expand', so a pattern bomb cannot run
+** unbounded C-side work without recursing through 'match' (script-sandbox-and-budgets
+** spec, "Pattern-matcher step budget").
+*/
+static void elysium_matchstep (MatchState *ms) {
+  if (l_unlikely(ms->steps == 0))
+    luaL_error(ms->L, "pattern too complex");
+  ms->steps--;
+}
+
+
 #define L_ESC		'%'
 #define SPECIALS	"^$*+?.([%-"
 
@@ -506,11 +520,15 @@
 static const char *max_expand (MatchState *ms, const char *s,
                                  const char *p, const char *ep) {
   ptrdiff_t i = 0;  /* counts maximum expand for item */
-  while (singlematch(ms, s + i, p, ep))
+  while (singlematch(ms, s + i, p, ep)) {
+    elysium_matchstep(ms);  /* Elysium: charge the greedy-scan loop (C22) */
     i++;
+  }
   /* keeps trying to match with the maximum repetitions */
   while (i>=0) {
-    const char *res = match(ms, (s+i), ep+1);
+    const char *res;
+    elysium_matchstep(ms);  /* Elysium: charge the backtrack loop (C22) */
+    res = match(ms, (s+i), ep+1);
     if (res) return res;
     i--;  /* else didn't match; reduce 1 repetition to try again */
   }
@@ -521,7 +539,9 @@
 static const char *min_expand (MatchState *ms, const char *s,
                                  const char *p, const char *ep) {
   for (;;) {
-    const char *res = match(ms, s, ep+1);
+    const char *res;
+    elysium_matchstep(ms);  /* Elysium: charge the lazy-expand loop (C22) */
+    res = match(ms, s, ep+1);
     if (res != NULL)
       return res;
     else if (singlematch(ms, s, p, ep))
@@ -568,6 +588,7 @@
 
 
 static const char *match (MatchState *ms, const char *s, const char *p) {
+  elysium_matchstep(ms);  /* Elysium: ELYSIUM_MATCH_STEPS budget (C22) */
   if (l_unlikely(ms->matchdepth-- == 0))
     luaL_error(ms->L, "pattern too complex");
   init: /* using goto to optimize tail recursion */
@@ -758,6 +779,9 @@
                        const char *s, size_t ls, const char *p, size_t lp) {
   ms->L = L;
   ms->matchdepth = MAXCCALLS;
+  ms->steps = ELYSIUM_MATCH_STEPS;  /* Elysium: budget is per library call (C22);
+                                     ** not reset by 'reprepstate', which runs once
+                                     ** per match attempt inside gmatch/gsub. */
   ms->src_init = s;
   ms->src_end = s + ls;
   ms->p_end = p + lp;
diff -ruN --exclude=elysium_internal.h --exclude=elysium_sandbox.c --exclude=elysium_shim.c --exclude=elysium_shim.h --exclude=module.modulemap a/ltable.c b/ltable.c
--- a/ltable.c	2026-08-22 13:16:03
+++ b/ltable.c	2026-08-22 13:53:49
@@ -179,8 +179,14 @@
       return hashpointer(t, f);
     }
     default: {
+      /* Elysium: hash by creation-order 'ordinal', not by address, so iteration
+      ** order over tables/closures/threads/userdata never depends on heap layout
+      ** (script-determinism spec). Light userdata and light C functions above are
+      ** unreachable from sandboxed scripts (reachability walk), so they are left
+      ** hashing by pointer.
+      */
       GCObject *o = gcvalue(key);
-      return hashpointer(t, o);
+      return hashmod(t, o->ordinal);
     }
   }
 }
diff -ruN --exclude=elysium_internal.h --exclude=elysium_sandbox.c --exclude=elysium_shim.c --exclude=elysium_shim.h --exclude=module.modulemap a/lundump.c b/lundump.c
--- a/lundump.c	2026-08-22 13:16:03
+++ b/lundump.c	2026-08-22 13:53:49
@@ -4,332 +4,32 @@
 ** See Copyright Notice in lua.h
 */
 
+/*
+** Elysium: this file is replaced in full by scripts/clua/elysium.patch. The stock
+** bytecode loader is an unverified binary format (crafted bytecode can corrupt the
+** VM) and every interpreter input in this build is untrusted script text, so binary
+** loading is removed outright rather than hardened. 'elysium_loadtext' already
+** refuses a chunk starting with the LUAC_DATA/binary signature before reaching here
+** (mode "t"); this stub is defence in depth for any other path that could reach
+** 'luaU_undump' (e.g. 'load' with a binary reader — 'load' itself is removed from
+** the sandbox, but the C entry point must still fail closed on its own).
+*/
+
 #define lundump_c
 #define LUA_CORE
 
 #include "lprefix.h"
 
-
-#include <limits.h>
-#include <string.h>
-
 #include "lua.h"
 
-#include "ldebug.h"
 #include "ldo.h"
-#include "lfunc.h"
-#include "lmem.h"
 #include "lobject.h"
-#include "lstring.h"
 #include "lundump.h"
 #include "lzio.h"
 
-
-#if !defined(luai_verifycode)
-#define luai_verifycode(L,f)  /* empty */
-#endif
-
-
-typedef struct {
-  lua_State *L;
-  ZIO *Z;
-  const char *name;
-} LoadState;
-
-
-static l_noret error (LoadState *S, const char *why) {
-  luaO_pushfstring(S->L, "%s: bad binary format (%s)", S->name, why);
-  luaD_throw(S->L, LUA_ERRSYNTAX);
+LClosure *luaU_undump (lua_State *L, ZIO *Z, const char *name) {
+  (void)Z;
+  luaO_pushfstring(L, "%s: binary chunks are disabled", name);
+  luaD_throw(L, LUA_ERRSYNTAX);
+  return NULL;  /* unreachable: luaD_throw never returns */
 }
-
-
-/*
-** All high-level loads go through loadVector; you can change it to
-** adapt to the endianness of the input
-*/
-#define loadVector(S,b,n)	loadBlock(S,b,(n)*sizeof((b)[0]))
-
-static void loadBlock (LoadState *S, void *b, size_t size) {
-  if (luaZ_read(S->Z, b, size) != 0)
-    error(S, "truncated chunk");
-}
-
-
-#define loadVar(S,x)		loadVector(S,&x,1)
-
-
-static lu_byte loadByte (LoadState *S) {
-  int b = zgetc(S->Z);
-  if (b == EOZ)
-    error(S, "truncated chunk");
-  return cast_byte(b);
-}
-
-
-static size_t loadUnsigned (LoadState *S, size_t limit) {
-  size_t x = 0;
-  int b;
-  limit >>= 7;
-  do {
-    b = loadByte(S);
-    if (x >= limit)
-      error(S, "integer overflow");
-    x = (x << 7) | (b & 0x7f);
-  } while ((b & 0x80) == 0);
-  return x;
-}
-
-
-static size_t loadSize (LoadState *S) {
-  return loadUnsigned(S, MAX_SIZET);
-}
-
-
-static int loadInt (LoadState *S) {
-  return cast_int(loadUnsigned(S, INT_MAX));
-}
-
-
-static lua_Number loadNumber (LoadState *S) {
-  lua_Number x;
-  loadVar(S, x);
-  return x;
-}
-
-
-static lua_Integer loadInteger (LoadState *S) {
-  lua_Integer x;
-  loadVar(S, x);
-  return x;
-}
-
-
-/*
-** Load a nullable string into prototype 'p'.
-*/
-static TString *loadStringN (LoadState *S, Proto *p) {
-  lua_State *L = S->L;
-  TString *ts;
-  size_t size = loadSize(S);
-  if (size == 0)  /* no string? */
-    return NULL;
-  else if (--size <= LUAI_MAXSHORTLEN) {  /* short string? */
-    char buff[LUAI_MAXSHORTLEN];
-    loadVector(S, buff, size);  /* load string into buffer */
-    ts = luaS_newlstr(L, buff, size);  /* create string */
-  }
-  else {  /* long string */
-    ts = luaS_createlngstrobj(L, size);  /* create string */
-    setsvalue2s(L, L->top.p, ts);  /* anchor it ('loadVector' can GC) */
-    luaD_inctop(L);
-    loadVector(S, getlngstr(ts), size);  /* load directly in final place */
-    L->top.p--;  /* pop string */
-  }
-  luaC_objbarrier(L, p, ts);
-  return ts;
-}
-
-
-/*
-** Load a non-nullable string into prototype 'p'.
-*/
-static TString *loadString (LoadState *S, Proto *p) {
-  TString *st = loadStringN(S, p);
-  if (st == NULL)
-    error(S, "bad format for constant string");
-  return st;
-}
-
-
-static void loadCode (LoadState *S, Proto *f) {
-  int n = loadInt(S);
-  f->code = luaM_newvectorchecked(S->L, n, Instruction);
-  f->sizecode = n;
-  loadVector(S, f->code, n);
-}
-
-
-static void loadFunction(LoadState *S, Proto *f, TString *psource);
-
-
-static void loadConstants (LoadState *S, Proto *f) {
-  int i;
-  int n = loadInt(S);
-  f->k = luaM_newvectorchecked(S->L, n, TValue);
-  f->sizek = n;
-  for (i = 0; i < n; i++)
-    setnilvalue(&f->k[i]);
-  for (i = 0; i < n; i++) {
-    TValue *o = &f->k[i];
-    int t = loadByte(S);
-    switch (t) {
-      case LUA_VNIL:
-        setnilvalue(o);
-        break;
-      case LUA_VFALSE:
-        setbfvalue(o);
-        break;
-      case LUA_VTRUE:
-        setbtvalue(o);
-        break;
-      case LUA_VNUMFLT:
-        setfltvalue(o, loadNumber(S));
-        break;
-      case LUA_VNUMINT:
-        setivalue(o, loadInteger(S));
-        break;
-      case LUA_VSHRSTR:
-      case LUA_VLNGSTR:
-        setsvalue2n(S->L, o, loadString(S, f));
-        break;
-      default: lua_assert(0);
-    }
-  }
-}
-
-
-static void loadProtos (LoadState *S, Proto *f) {
-  int i;
-  int n = loadInt(S);
-  f->p = luaM_newvectorchecked(S->L, n, Proto *);
-  f->sizep = n;
-  for (i = 0; i < n; i++)
-    f->p[i] = NULL;
-  for (i = 0; i < n; i++) {
-    f->p[i] = luaF_newproto(S->L);
-    luaC_objbarrier(S->L, f, f->p[i]);
-    loadFunction(S, f->p[i], f->source);
-  }
-}
-
-
-/*
-** Load the upvalues for a function. The names must be filled first,
-** because the filling of the other fields can raise read errors and
-** the creation of the error message can call an emergency collection;
-** in that case all prototypes must be consistent for the GC.
-*/
-static void loadUpvalues (LoadState *S, Proto *f) {
-  int i, n;
-  n = loadInt(S);
-  f->upvalues = luaM_newvectorchecked(S->L, n, Upvaldesc);
-  f->sizeupvalues = n;
-  for (i = 0; i < n; i++)  /* make array valid for GC */
-    f->upvalues[i].name = NULL;
-  for (i = 0; i < n; i++) {  /* following calls can raise errors */
-    f->upvalues[i].instack = loadByte(S);
-    f->upvalues[i].idx = loadByte(S);
-    f->upvalues[i].kind = loadByte(S);
-  }
-}
-
-
-static void loadDebug (LoadState *S, Proto *f) {
-  int i, n;
-  n = loadInt(S);
-  f->lineinfo = luaM_newvectorchecked(S->L, n, ls_byte);
-  f->sizelineinfo = n;
-  loadVector(S, f->lineinfo, n);
-  n = loadInt(S);
-  f->abslineinfo = luaM_newvectorchecked(S->L, n, AbsLineInfo);
-  f->sizeabslineinfo = n;
-  for (i = 0; i < n; i++) {
-    f->abslineinfo[i].pc = loadInt(S);
-    f->abslineinfo[i].line = loadInt(S);
-  }
-  n = loadInt(S);
-  f->locvars = luaM_newvectorchecked(S->L, n, LocVar);
-  f->sizelocvars = n;
-  for (i = 0; i < n; i++)
-    f->locvars[i].varname = NULL;
-  for (i = 0; i < n; i++) {
-    f->locvars[i].varname = loadStringN(S, f);
-    f->locvars[i].startpc = loadInt(S);
-    f->locvars[i].endpc = loadInt(S);
-  }
-  n = loadInt(S);
-  if (n != 0)  /* does it have debug information? */
-    n = f->sizeupvalues;  /* must be this many */
-  for (i = 0; i < n; i++)
-    f->upvalues[i].name = loadStringN(S, f);
-}
-
-
-static void loadFunction (LoadState *S, Proto *f, TString *psource) {
-  f->source = loadStringN(S, f);
-  if (f->source == NULL)  /* no source in dump? */
-    f->source = psource;  /* reuse parent's source */
-  f->linedefined = loadInt(S);
-  f->lastlinedefined = loadInt(S);
-  f->numparams = loadByte(S);
-  f->is_vararg = loadByte(S);
-  f->maxstacksize = loadByte(S);
-  loadCode(S, f);
-  loadConstants(S, f);
-  loadUpvalues(S, f);
-  loadProtos(S, f);
-  loadDebug(S, f);
-}
-
-
-static void checkliteral (LoadState *S, const char *s, const char *msg) {
-  char buff[sizeof(LUA_SIGNATURE) + sizeof(LUAC_DATA)]; /* larger than both */
-  size_t len = strlen(s);
-  loadVector(S, buff, len);
-  if (memcmp(s, buff, len) != 0)
-    error(S, msg);
-}
-
-
-static void fchecksize (LoadState *S, size_t size, const char *tname) {
-  if (loadByte(S) != size)
-    error(S, luaO_pushfstring(S->L, "%s size mismatch", tname));
-}
-
-
-#define checksize(S,t)	fchecksize(S,sizeof(t),#t)
-
-static void checkHeader (LoadState *S) {
-  /* skip 1st char (already read and checked) */
-  checkliteral(S, &LUA_SIGNATURE[1], "not a binary chunk");
-  if (loadByte(S) != LUAC_VERSION)
-    error(S, "version mismatch");
-  if (loadByte(S) != LUAC_FORMAT)
-    error(S, "format mismatch");
-  checkliteral(S, LUAC_DATA, "corrupted chunk");
-  checksize(S, Instruction);
-  checksize(S, lua_Integer);
-  checksize(S, lua_Number);
-  if (loadInteger(S) != LUAC_INT)
-    error(S, "integer format mismatch");
-  if (loadNumber(S) != LUAC_NUM)
-    error(S, "float format mismatch");
-}
-
-
-/*
-** Load precompiled chunk.
-*/
-LClosure *luaU_undump(lua_State *L, ZIO *Z, const char *name) {
-  LoadState S;
-  LClosure *cl;
-  if (*name == '@' || *name == '=')
-    S.name = name + 1;
-  else if (*name == LUA_SIGNATURE[0])
-    S.name = "binary string";
-  else
-    S.name = name;
-  S.L = L;
-  S.Z = Z;
-  checkHeader(&S);
-  cl = luaF_newLclosure(L, loadByte(&S));
-  setclLvalue2s(L, L->top.p, cl);
-  luaD_inctop(L);
-  cl->p = luaF_newproto(L);
-  luaC_objbarrier(L, cl, cl->p);
-  loadFunction(&S, cl->p, NULL);
-  lua_assert(cl->nupvalues == cl->p->sizeupvalues);
-  luai_verifycode(L, cl->p);
-  return cl;
-}
-
diff -ruN --exclude=elysium_internal.h --exclude=elysium_sandbox.c --exclude=elysium_shim.c --exclude=elysium_shim.h --exclude=module.modulemap a/lvm.c b/lvm.c
--- a/lvm.c	2026-08-22 13:16:03
+++ b/lvm.c	2026-08-22 13:53:49
@@ -374,7 +374,8 @@
 ** Compare two strings 'ts1' x 'ts2', returning an integer less-equal-
 ** -greater than zero if 'ts1' is less-equal-greater than 'ts2'.
 ** The code is a little tricky because it allows '\0' in the strings
-** and it uses 'strcoll' (to respect locales) for each segment
+** and it uses 'strcmp' (Elysium: byte order, not locale-dependent
+** 'strcoll' — script-determinism spec) for each segment
 ** of the strings. Note that segments can compare equal but still
 ** have different lengths.
 */
@@ -384,7 +385,7 @@
   const char *s2 = getstr(ts2);
   size_t rl2 = tsslen(ts2);
   for (;;) {  /* for each segment */
-    int temp = strcoll(s1, s2);
+    int temp = strcmp(s1, s2);  /* Elysium: strcmp, not strcoll */
     if (temp != 0)  /* not equal? */
       return temp;  /* done */
     else {  /* strings are equal up to a '\0' */
@@ -669,6 +670,14 @@
           luaG_runerror(L, "string length overflow");
         }
         tl += l;
+      }
+      /* Elysium: ELYSIUM_MAX_STRING (262,144 bytes) bounds every script-visible
+      ** string; '..' is otherwise the only uncapped producer (C22, script-sandbox-
+      ** and-budgets spec, "Pattern-matcher step budget and library input caps").
+      */
+      if (l_unlikely(tl > ELYSIUM_MAX_STRING)) {
+        L->top.p = top - total;  /* pop strings to avoid wasting stack */
+        luaG_runerror(L, "string too long");
       }
       if (tl <= LUAI_MAXSHORTLEN) {  /* is result a short string? */
         char buff[LUAI_MAXSHORTLEN];
